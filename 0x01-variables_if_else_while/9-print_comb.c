#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int o = '0';
	int i = '0';
	int g = '9';

	putchar(o, );
	for (i = '0'; i <= '8'; i++)
	{
		putchar(i+1, );
	}
	putchar(g)
	putchar('\n');
	return (0);
}
