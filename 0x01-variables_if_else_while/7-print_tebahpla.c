#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char z = 'z';

	for (z >= 'a')
	{
		putchar(z);
	}
	putchar('\n');
	return (0);
}
